import React, {Component} from 'react';

class CropSelector extends Component {

  constructor(props) {
    super(props);
    console.log('croppy', this.props);
  }

  render() {
    return (
      <div>selector</div>
    );
  }
}

export default CropSelector;
